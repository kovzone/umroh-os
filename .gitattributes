# Normalize text files to LF in repository across OS/editor setups.
* text=auto eol=lf

# Windows-native scripts should keep CRLF for local execution ergonomics.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
