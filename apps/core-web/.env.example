# Browser-side base URL for gateway-svc. Used by src/lib/api/gateway/client.ts.
# In dev: the browser hits the host-mapped port. In prod: the deployed gateway URL.
VITE_GATEWAY_URL=http://localhost:4000
